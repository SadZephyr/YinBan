# AI影伴 — 项目文档

---

## （注：该文档的例子都由AI生成 仅为参考 不代表真实情况 项目有推进后管理员会更新）

## 一、项目介绍

为轻中度成年自闭症患者打造的可穿戴AI辅助工具，部分替代"影子老师"功能。

**目标用户**：具有一定语言理解能力、可进行基础社会活动、但在社交应答和情境判断上存在困难的轻中度成年自闭症患者。

**使用方式**：患者佩戴摄像头（胸前）+ 骨传导耳机外出，ESP32-CAM 通过 WiFi 直推视频流到服务器，手机 App 走 WebSocket 信令通道，AI 分析后：

- 正常情况 → 耳机给患者语音提示（辅助社交）
- 检测到异常 → 推送预警到监护人手机

**两大核心功能**：

| 功能  | 说明                                |
| --- | --------------------------------- |
| 预警  | AI检测到异常行为/情绪/环境风险 → 监护人端App弹窗+通知栏 |
| 陪伴  | 社交场景需要辅助 → 患者端耳机播放AI语音 + 屏幕显示文字   |

---

## 二、项目框架

### 完整数据流

```
╔══════════════════════ 上行：数据采集 ══════════════════════╗
║                                                          ║
║  [ESP32-CAM]────RTMP推流(WiFi)────┐                      ║
║         (胸前摄像头)               │                      ║
║                                   ▼                      ║
║  [骨传导耳机]───蓝牙──→[患者手机]──WebSocket──→  ┌─────────┐ ║
║   (麦克风采音)          (Kotlin)               │ 服务器   │ ║
║                            │                   │(FastAPI │ ║
║                            │ GPS/设备状态/SOS   │ + SRS)  │ ║
║                            └──WebSocket──────→  │         │ ║
║                                                 └────┬────┘ ║
╚══════════════════════════════════════════════════════╪══════╝
                                                       │
                                                    AI Pipeline
                                                 (ASR语音→视觉→LLM→TTS)
                                                       │
╔══════════════════════ 下行：AI结果下发 ═══════════════╪══════╗
║                                                       │      ║
║       ┌───────────────────────────────────────────────┘      ║
║       │                                                      ║
║       ▼                                                      ║
║  ┌─────────┐                                                 ║
║  │ 服务器   │                                                 ║
║  └────┬────┘                                                 ║
║       │                                                      ║
║       ├──WebSocket──→[监护人手机]  预警弹窗 + 定位 + 消息     ║
║       │              (Kotlin)                                ║
║       │                                                      ║
║       ├──SRS RTMP──→[监护人手机]  实时视频画面                 ║
║       │                                                   ║
║       └──WebSocket──→[患者手机]──TTS合成──蓝牙──→[骨传导耳机] ║
║                       AI语音指令           (患者听到提示)     ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝
```

### 一句话总结

> **所有数据汇总到服务器 → AI 分析 → 预警推给监护人、提示推给患者。**
> 视频走 RTMP（ESP32-CAM 直连），音频和信令走 WebSocket（手机中转）。

### 数据传输链路

| 方向  | 内容           | 路径                                       |
| --- | ------------ | ---------------------------------------- |
| 上行  | 视频画面         | ESP32-CAM → RTMP(WiFi) → 服务器 SRS         |
| 上行  | 环境音频         | 骨传导耳机麦克风 → 蓝牙 → 患者手机 → WebSocket → 服务器   |
| 上行  | GPS/设备状态/SOS | 患者手机 → WebSocket → 服务器                   |
| 下行  | AI语音提示       | 服务器 → WebSocket → 患者手机(TTS) → 蓝牙 → 骨传导耳机 |
| 下行  | 预警通知         | 服务器 → WebSocket → 监护人手机                  |
| 下行  | 实时画面         | 服务器 SRS → RTMP → 监护人手机                   |
| 双向  | 心跳/配对/消息     | 双端 ↔ WebSocket ↔ 服务器                     |

**硬件选型**：

| 设备  | 型号                           | 价格       |
| --- | ---------------------------- | -------- |
| 摄像头 | ESP32-CAM（WiFi推流，640×480）    | ¥30-60   |
| 耳机  | 韶音 OpenMove 骨传导（不入耳，能同时听环境音） | ¥300-600 |

**连接方式**：ESP32-CAM 直连服务器 SRS 推流（RTMP，同 WiFi）→ 患者端输入房间号连 WebSocket ← 监护人端输入同一房间号 → 自动配对。视频流走 SRS，信令走 WebSocket。

### 前端底部导航栏

App 底部四个 Tab，从左到右依次：

| 位置 | 栏目 | 说明 |
|:---:|------|------|
| 1 | **主页** | 不需连接监护人就能使用的基本功能：设备状态总览、快捷 SOS、连接状态 |
| 2 | **AI聊天（小影火）** | 半 Agent 对话栏目，接入 DeepSeek V4 / 通义千问。能力：分析患者当前状态、辅助社交治疗、日常聊天陪伴。可主动推送提醒和安抚 |
| 3 | **监护栏** | 与监护人交互的核心入口：收发消息、管理摄像头/耳机权限、查看位置共享、通话请求 |
| 4 | **我的** | 个人中心：设置头像昵称、调节提示音量/振动/隐私模式、查看设备电量、关于与反馈 |

> **"小影火"定位**：介于 AI 陪伴和 AI 治疗师之间。不同于通用聊天机器人，它会结合上下文（位置、时间、近期情绪、设备状态）主动分析和辅助，像一个随身携带的"影子老师"的 AI 分身。

---

## 三、基础版和完整版

### 基础版

**目标**：端到端走通核心链路。

| 模块   | 内容                    |
| ---- | --------------------- |
| 视频   | 摄像头画面推到监护人端（延迟<5s）    |
| 连接   | 房间号配对，患者和监护人互通        |
| 消息   | WebSocket收发消息（断线自动重连） |
| 定位   | 监护人端地图显示患者位置          |
| 设备控制 | 摄像头/耳机独立开关            |
| AI预警 | ≥3种预警场景稳定触发           |
| AI陪伴 | ≥3种陪伴场景生成提示并语音播放      |

**不做**：数据库、用户系统、注册登录、绑定码、历史记录、数据看板、个性化学习、一键通话、日程辅助、iOS适配、离线缓存、端到端加密。

### 完整版（远期）

在基础版之上增加：

| 功能    | 说明                      |
| ----- | ----------------------- |
| 用户系统  | 注册登录、多患者绑定、权限管理         |
| 数据库   | SQLite存储历史记录、预警日志       |
| 数据看板  | 情绪趋势图、社交统计、预警回顾、周报导出    |
| 个性化学习 | AI学习患者触发点、有效安抚方式、自定义话术库 |
| 一键通话  | 预警时监护人一键呼叫患者            |
| 日程辅助  | 监护人预设日程，AI到时间提醒         |
| 隐私保护  | 端到端加密、路人脸模糊、数据自动过期      |
| 智能手表端 | 心率/皮电采集、跌倒检测            |
| 多语言   | 界面及AI提示多语言切换            |
| iOS适配 | 支持iPhone                |

---

## 四、技术栈分配

### 技术栈总览

| 技术                | 用途             | 难度  |
| ----------------- | -------------- |:---:|
| Kotlin (Android)  | 手机App          | ⭐⭐  |
| Python FastAPI    | 消息转发服务器        | ⭐⭐  |
| Python 调API       | AI分析（调云端大模型接口） | ⭐⭐  |
| SRS (Windows版)    | 视频流中转，解压即用     | ⭐   |
| Cloudflare Tunnel | 让外网手机能连本地电脑    | ⭐   |

基础版只用这5个。数据库、云服务器、Docker、Nginx、Linux 均不需要。

### 五人分工

| 人员  | 角色         | 技术             | 具体产出                                               |
|:---:| ---------- | -------------- | -------------------------------------------------- |
| A   | 前端架构 + 患者端 | Kotlin/Android | `app/…/network/`（共享模块）+ `ui/PatientActivity`（患者UI） |
| B   | 前端监护人端     | Kotlin/Android | `ui/GuardianActivity`（监护人UI），使用A的共享模块              |
| C   | 后端服务器      | Python FastAPI | WebSocket消息转发 + 房间管理                               |
| D   | AI流水线      | Python         | ASR/视觉/LLM/TTS 调用 + 预警决策 + 陪伴提示生成                  |
| E   | 全栈集成 + 部署  | 全栈             | SRS部署 + Cloudflare Tunnel + 端到端测试 + UI辅助           |

### AI服务选型

| 能力       | 服务                  | 说明 |
| -------- | ------------------- |------|
| ASR语音转文字 | 阿里云实时语音识别 / 讯飞      | |
| 视觉理解     | 通义千问VL / GLM-4V     | |
| 文本分析     | DeepSeek API / 通义千问 | |
| 小影火对话    | DeepSeek V4（API） | 半 Agent，支持状态分析、辅助治疗、日常聊天，结合上下文主动推送 |
| TTS语音合成  | 火山引擎 / 微软Azure TTS  | |

### 三套接口

**接口A：App ↔ 服务器（WebSocket JSON）**

连接地址：`ws://<IP>:8000/ws?room=<ROOM_ID>&role=<patient|guardian>`

消息格式：

```json
{"msg_id": "uuid", "type": "类型", "data": {}, "timestamp": "ISO8601"}
```

消息类型：

| type                   | 方向         | 说明          |
| ---------------------- | ---------- | ----------- |
| join_room              | App→Server | 加入房间        |
| room_status            | Server→App | 房间状态变化      |
| location_update        | 患者→服务器→监护人 | GPS位置       |
| stream_start           | 患者→服务器     | 开始推流(信令)    |
| stream_url             | 服务器→监护人    | 推流地址        |
| device_status          | 患者→服务器→监护人 | 设备开关状态      |
| ai_voice_command       | 服务器→患者     | AI提示(文字+语音) |
| alert                  | 服务器→监护人    | 预警通知        |
| manual_message         | 监护人→患者     | 手动发消息       |
| device_control_request | 监护人→患者     | 请求开关设备      |
| ping / pong            | 双向         | 心跳          |

> 注：视频帧和音频片段通过 SRS RTMP 推流直连，不走 WebSocket。

**接口B：服务器 ↔ AI Pipeline（Python函数调用）**

C的代码 `from ai import` 调用D写的三个函数：

```python
# 视频帧分析（每2秒一次）
def analyze_frame(image_bytes: bytes, patient_id: str, context: dict) -> AnalysisResult

# 音频分析（每5秒一次）
def analyze_audio(audio_bytes: bytes, patient_id: str, context: dict) -> AnalysisResult

# 综合风险评估
def assess_risk(patient_id: str, recent_frames: list, recent_asr_text: list,
                location: dict, device_status: dict) -> AnalysisResult

# 返回结构
class AnalysisResult:
    alert: Optional[Alert]    # 有则推给监护人
    prompt: Optional[Prompt]  # 有则推给患者

class Alert:
    type: str        # emotion | conflict | stagnation | environment | geofence | device
    severity: str    # red | yellow | green
    message: str

class Prompt:
    text: str
    audio_base64: Optional[str]  # TTS合成的语音
```

**接口C：前端共享模块（A写，B用）**

```
app/src/main/java/com/yinban/ai/
├── network/
│   ├── MessageModels.kt     # 统一消息外壳 + 14种Data类
│   └── WebSocketManager.kt  # WS单例（connect/disconnect/send/messageStream/心跳）
├── hardware/
│   └── HardwareStreamManager.kt  # 硬件流接口
├── storage/
│   └── PreferenceManager.kt     # SharedPreferences持久化
└── utils/
    └── LocationPrivacy.kt       # 位置隐私模糊化
```

### 预警场景（6种）

| 级别  | 场景    | 触发条件             |
|:---:| ----- | ---------------- |
| 🔴  | 情绪异常  | 面部表情痛苦/愤怒、语音语调异常 |
| 🔴  | 社交冲突  | 对方声音大/争吵、患者躲闪    |
| 🔴  | 环境风险  | 道路中央、施工区域等危险场景   |
| 🟡  | 停滞异常  | 同一地点停留超过30分钟     |
| 🟡  | 远离安全区 | GPS超出电子围栏        |
| 🟢  | 设备异常  | 摄像头遮挡、耳机脱落、断网    |

### 陪伴提示场景（7种）

社交问候 / 排队等待安抚 / 嘈杂环境安抚 / 买单付款提醒 / 过马路提醒 / 公交地铁提醒 / 情绪安抚

---

## 五、项目特点

**1. 不做数据库**

基础版不用任何数据库。通过房间号直接配对，像进Zoom会议室。不需要用户注册、登录、绑定、历史记录。数据全部实时流转，不落盘存储。

**2. Windows笔记本当服务器**

不租云服务器。用一台Windows笔记本运行 Python FastAPI + SRS 流媒体服务。不需要学 Linux、SSH、Docker、Nginx、安全组配置。调试时直接在本机看日志，出问题立刻改。

**3. 设备、手机、电脑同WiFi**

同一局域网下，手机直接连电脑IP（如 `192.168.1.100:8000`）通信。视频推流走局域网，延迟低、不消耗手机流量。需要外出演示时，用 Cloudflare Tunnel（免费）获得公网域名，手机用4G也能连回电脑。

**4. 设备独立开关，患者最终控制**

摄像头和耳机在App中可分别独立开关。四种组合：全开、关摄像头（需隐私）、关耳机（需安静）、全关（休息）。患者拥有最终控制权，监护人可远程请求开启但需患者确认。

**5. 硬件直连服务器，手机走信令**

ESP32-CAM 通过同 WiFi 的 RTMP 推流直连服务器（SRS），不经过手机中转。患者手机只走 WebSocket 信令通道（配对、SOS、位置上报、设备控制、AI 指令）。视频流走数据面（硬件直连），控制面走手机，两路分离。

好处：减少一跳延迟、手机省电省带宽、架构简单。仅在演示/同局域网场景下使用；若需远程公网访问，可通过 Cloudflare Tunnel 暴露服务器。

**6. 接口先行，并行开发**

App ↔ 服务器（WebSocket JSON）、服务器 ↔ AI（Python函数调用）、前端内部（Kotlin共享模块）三套接口在开发前冻结。按接口独立开发，各自写自己的部分，最后拼在一起。改接口必须全员通知。
